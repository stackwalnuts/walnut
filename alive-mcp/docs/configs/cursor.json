{
  "_comment": "Cursor config. Lives at <project>/.cursor/mcp.json (project scope) or ~/.cursor/mcp.json (global). Cursor auto-reloads on file change. Replace ALIVE_WORLD_ROOT with the absolute path to your World (the directory containing .alive/).",
  "mcpServers": {
    "alive": {
      "command": "uvx",
      "args": ["alive-mcp@0.1.0"],
      "env": {
        "ALIVE_WORLD_ROOT": "/absolute/path/to/your/world"
      }
    }
  }
}
