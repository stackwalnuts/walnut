{
  "_comment": "Gemini CLI config. Lives at ~/.gemini/settings.json (global) or <project>/.gemini/settings.json (project). Run `gemini mcp list` to verify after editing. Replace ALIVE_WORLD_ROOT with the absolute path to your World (the directory containing .alive/).",
  "mcpServers": {
    "alive": {
      "command": "uvx",
      "args": ["alive-mcp@0.1.0"],
      "env": {
        "ALIVE_WORLD_ROOT": "/absolute/path/to/your/world"
      }
    }
  }
}
