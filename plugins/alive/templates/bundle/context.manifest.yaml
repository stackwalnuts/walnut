---
name: "{{name}}"
goal: "{{goal}}"
species: "{{species}}"  # outcome | evergreen
phase: draft  # draft | prototype | published | done
sensitivity: "{{sensitivity}}"  # open | private | restricted
version: "0.1"
due:  # optional — ISO date. Bundle-scoped tasks can inherit this deadline.
created: "{{date}}"
session: "{{session_id}}"
parent_bundle:

description:
author:
tags: []
price: free
license: MIT

installs:
  skills: []
  hooks: []
  templates: []

people: []
shared: []

tasks_total: 0
tasks_done: 0
tasks_created:

context_routes: []

discovered:
  status: none
  last_mined:
  processed: []
  unprocessed: []

observations: []

creates: []
---
